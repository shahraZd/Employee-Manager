/*
 * LoadingWrapper Messages
 *
 * This contains all the text for the LoadingWrapper component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.LoadingWrapper'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoadingWrapper component!',
  },
})
